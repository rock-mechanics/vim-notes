#!/usr/bin/bash

# expands to environment variable.

echo we can expands to environment variable using bash expansion

echo all env variables starting from BASH

echo -e "\n"

for i in ${!BASH*}
do
	echo $i ___ ${!i}
done

# if given a variable, bash use ${!variable} to expand the outcome of the variable.
